name: zedtutors
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^2.27.0
  firebase_auth: ^4.18.0
  cloud_firestore: ^4.15.8
  # Additional dependencies for the project plan
  firebase_ui_auth: ^1.13.0
  google_sign_in: ^6.3.0
  file_picker: ^6.2.0
  http: ^1.2.1
  flutter_lints: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/zedtutors_logo.png
    - assets/images/zedtutors_background.png
